{
  "framework": null,
  "installCommand": "if [ ! -d \"$HOME/flutter\" ]; then git clone https://github.com/flutter/flutter.git -b stable --depth 1 \"$HOME/flutter\"; fi && export PATH=\"$PATH:$HOME/flutter/bin\" && flutter precache --web && flutter pub get",
  "buildCommand": "export PATH=\"$PATH:$HOME/flutter/bin\" && flutter build web --release --dart-define=API_BASE_URL=$API_BASE_URL",
  "outputDirectory": "build/web",
  "routes": [
    { "src": "/(.*)", "dest": "/index.html", "status": 200 }
  ]
}
