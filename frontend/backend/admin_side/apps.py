from django.apps import AppConfig

class AdminSideConfig(AppConfig):
    name = 'admin_side'  # The name should be the exact name of your app
    verbose_name = "Admin Side"  # Optional: Display name for your app in Django admin
